// Generated by CoffeeScript 1.6.3
(function() {
  var Ball, COL_COUNT, GRID_SIZE, GameState, LINE_COUNT;

  GRID_SIZE = 25;

  COL_COUNT = 9;

  LINE_COUNT = 9;

  Ball = gamvas.Actor.extend({
    create: function(name, x, y) {
      var curState;
      this._super(name, x, y);
      curState = gamvas.state.getCurrentState();
      return this.setFile(curState.getImage('ball.png'));
    }
  });

  GameState = gamvas.State.extend({
    init: function() {
      return this.addBalls(5);
    },
    addBall: function() {
      var x, y;
      x = Math.floor(Math.random() * COL_COUNT);
      y = Math.floor(Math.random() * LINE_COUNT);
      return this.addActor(new Ball(false, x * GRID_SIZE, y * GRID_SIZE));
    },
    draw: function() {}
  });

  gamvas.event.addOnload(function() {
    gamvas.state.addState(new GameState('game'));
    return gamvas.start('lines-canvas', false);
  });

}).call(this);
